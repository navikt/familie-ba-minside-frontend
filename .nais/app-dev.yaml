apiVersion: 'nais.io/v1alpha1'
kind: 'Application'
metadata:
  name: familie-ba-minside-frontend
  namespace: teamfamilie
  labels:
    team: teamfamilie
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: '25M'
    nginx.ingress.kubernetes.io/proxy-read-timeout: '120'
spec:
  envFrom:
    - secret: familie-ba-minside-frontend-unleash-api-token
  image: {{ image }}
  replicas:
    min: 2
    max: 2
    cpuThresholdPercentage: 50
  port: 9000
  liveness:
    path: /barnetrygd/min-barnetrygd/api/internal/isAlive
    initialDelay: 5
  readiness:
    path: /barnetrygd/min-barnetrygd/api/internal/isReady
    initialDelay: 5
  observability:
    autoInstrumentation:
      enabled: true
      runtime: nodejs
  prometheus:
    enabled: true
    path: /barnetrygd/min-barnetrygd/api/internal/metrics
  ingresses:
    - 'https://familie-ba-minside.intern.dev.nav.no/barnetrygd/min-barnetrygd'
  accessPolicy:
    outbound:
      rules:
        - application: nav-dekoratoren
          namespace: personbruker
      external:
        - host: teamfamilie-unleash-api.nav.cloud.nais.io
  resources:
    limits:
      cpu: 2000m
      memory: 1024Mi
    requests:
      memory: 512Mi
  idporten:
    enabled: true
    sidecar:
      enabled: true
      autoLogin: true
      level: Level4
      autoLoginIgnorePaths:
        - /barnetrygd/min-barnetrygd/**/*.css
        - /barnetrygd/min-barnetrygd/**/manifest.json
  tokenx:
    enabled: true
  secureLogs:
    enabled: true
  env:
    - name: ENV
      value: dev
